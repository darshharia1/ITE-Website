/* =====================================================
   ITE STARTUP LAUNCH PAD – CONFIG (config.js)
   ===================================================== */
window.ITE = window.ITE || {};

window.ITE.Config = {
  API_BASE_URL: 'http://localhost:3002' // Update to live AWS App Runner URL when deployed
};
